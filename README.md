# uploadhtmlskill
Use when publishing, sharing, uploading, or hosting an HTML page (single-file HTML or a static site as HTML/CSS/JS or .zip) as a shareable link, or when the user asks to "post to 100mini" / "生成分享链接" / "上传到 100mini" / "发布学习页面". Use to upload from the command line without a browser or login.
